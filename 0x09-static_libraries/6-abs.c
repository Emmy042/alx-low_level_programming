#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - gives the absolute value of a number
 * @n: The character to check
 *
 * Return: returns the final result
 */

int _abs(int n)
{

int result;
result = abs(n);

return (result);

}
