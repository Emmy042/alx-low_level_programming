#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - Gets the length of a string
 * @s: The string to evaluate
 *
 * Return: The length of the string
 */


int _strlen(char *s)
{

int length = strlen(s);

return (length);

}
