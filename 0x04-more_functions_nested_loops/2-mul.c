#include <stdio.h>
#include <ctype.h>
#include "main.h"

/**
 * mul - multiples 2 numbers
 * @a: The 1st number
 * @b: the 2nd number
 *
 * Return: gets the final result
 */

int mul(int a, int b)
{
int result;
result = a * b;

return (result);
}
