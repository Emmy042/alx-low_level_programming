#include <stdio.h>
#include "main.h"

/**
 * _puts - Prints a string followed by a new line to stdout
 * @str: Pointer to the string to be printed
 *
 * Return: Nothing (void)
 */

void _puts(char *str)
{

puts(str);

}
